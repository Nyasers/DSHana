# 依赖与构建产物
.pnpm-store
node_modules/
dist/
releases/

# 临时文件（pack 中间原料 .tmp/pkg、测试产物 .tmp/tests 等）
.tmp/

# 系统杂项
*.log
.DS_Store

# SDD 本地 spec（不进 git，仅本地规划产物）
SPECS.md
specs/

data/

# 运行期 node 代理（installDepsFromPlugin 写入插件根，每次安装幂等重建；不进 git）
# /node 为 Linux/macOS 无扩展名形态；仓库根无同名资产，安全忽略
/node.cmd
/node

